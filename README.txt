README.txt  file describing how to use the software (i.e. at least: how to run the code that creates the index, how to start the search interface and how to perform a simple query). 

Instructions: 

Index creation:
To create the index run the following code below in the terminal:
python Indexer.py

Search:
To start the search interface run the following code below in the terminal:
python Search.py

After running Search.py, To perform a simple query -> type your query after the prompt - Enter a query: 


Extra credit:
Also we created a GUI using tkinter for the GUI, run the following code to open the GUI for search 
python Gui.py 
Type in the query box & click the search button

Detect and eliminate duplicate pages (defrag) 

Enhance the index with word positions and use them for retrieval. 
